DATABASE_URL="file:./dev.db"

# Ed25519 signing keys (hex format)
# Generate with: import { generateKeyPair } from '@human-stamp/core'; console.log(await generateKeyPair());
# REQUIRED in production, optional in dev (will use ephemeral keys with warnings)
SIGNING_PRIVATE_KEY=""
SIGNING_PUBLIC_KEY=""

# Optional: Override ffmpeg/ffprobe paths (defaults to bundled binaries)
# FFMPEG_PATH="/usr/bin/ffmpeg"
# FFPROBE_PATH="/usr/bin/ffprobe"
